import { ResultsPerPage } from '@/components/molecules/ResultsPerPage';

export const ResultsSettings = () => {
  return (
    <div className='flex items-center gap-10'>
      <ResultsPerPage />
    </div>
  );
};
