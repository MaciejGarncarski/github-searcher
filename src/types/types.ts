import { COMPATIBLE_COLORS } from '@/constants/AccentColors';
import { COMPATIBLE_THEMES } from '@/constants/Themes';

export type Color = typeof COMPATIBLE_COLORS[number];
export type ThemeColor = typeof COMPATIBLE_THEMES[number];
